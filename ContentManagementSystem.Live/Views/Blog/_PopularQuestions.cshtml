@if (Model.Count > 0)
{
    <div class="well">
        <ul class="nav">
            <li class="nav-header">Popular Posts</li>
            @foreach (SelectListItem question in Model)
            {
                <li><a href="/blog/@question.Value">@question.Text</a></li>
            }
        </ul>
    </div>
}
